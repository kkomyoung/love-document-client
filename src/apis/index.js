import { login, register, getUserInfo } from './user'
import { getCategories, postQuestions } from './category'

export {
  // User
  login,
  register,
  getUserInfo,

  // Category
  getCategories,
  postQuestions,
}
